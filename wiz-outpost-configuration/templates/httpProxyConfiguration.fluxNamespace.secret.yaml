{{- if and .Values.httpProxyConfiguration.enabled .Values.httpProxyConfiguration.createFluxNamespaceCopy }}
{{- $nsPrefix := (index (splitList "-" .Release.Namespace) 0) }}
{{- $autoFluxNamespace := ternary "flux-system" (printf "%s-flux-system" $nsPrefix) (eq .Release.Namespace "default") }}
{{- $fluxNamespace := default $autoFluxNamespace .Values.outpostController.wizFluxNamespace }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ .Values.httpProxyConfiguration.name }}
  namespace: {{ $fluxNamespace }}
  labels:
    {{- include "wiz-outpost-configuration.labels" . | nindent 4 }}
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
data:
  {{- include "wiz-outpost-configuration.httpProxyConfiguration.data" . | nindent 2 }}
{{- end }}
