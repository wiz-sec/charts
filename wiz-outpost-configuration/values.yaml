# Default values for wiz-outpost-configuration.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

httpProxyConfiguration:
  enabled: false
  name: http-proxy-configuraion
  httpProxy: replaceme
  httpsProxy: replaceme
  noProxy:
  - replaceme
  - replaceme2
