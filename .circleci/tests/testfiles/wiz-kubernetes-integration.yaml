global:
  wizApiToken:
    clientId: "abcd"
    clientToken: "abcd"
  imagePullSecrets:
  - name: aaaa

httpProxyConfiguration:
  enabled: true
  httpsProxy: "https://test.com"

wiz-kubernetes-connector:
  enabled: true
  autoCreateConnector:
    connectorName: "test"
    clusterFlavor: "eks"

  broker:
    enabled: true

wiz-admission-controller:
  enabled: true
  replicaCount: 10
